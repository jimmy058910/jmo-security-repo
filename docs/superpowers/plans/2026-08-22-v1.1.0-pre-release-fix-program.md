# v1.1.0 pre-release fix program

> **For agentic workers:** this is a **program** plan, not a task plan. It fixes
> the order and the boundaries; each phase gets its **own** measured plan written
> at the time it is executed, after the measuring described in that phase's
> "Measure first" block. Do **not** expand a phase into TDD tasks from this
> document alone — see [Why there are no task-level steps here](#why-there-are-no-task-level-steps-here).

**Goal:** fix every open defect in `jmo-security` before tagging v1.1.0, in an
order where each phase's verification can be trusted and no phase invalidates an
earlier one.

**Architecture:** phases are ordered by three constraints, in priority order —
**instruments before measurements**, **delete before refactor**, and
**upstream before downstream** along the data pipeline
(`adapters → fingerprint dedup → enrich → compliance → EPSS → suppression → reporters`).
The pipeline constraint is the audit campaign's own hard-won ordering lesson,
applied to fixes instead of audits.

**Tech Stack:** Python 3.12, pytest (+xdist, +split), uv/`uv.lock`, GitHub
Actions, Docker (WSL only on this machine), `gh` CLI.

**Spec:** [2026-08-08-v1.1.0-audit-campaign.md](2026-08-08-v1.1.0-audit-campaign.md)
(release bar and cadence) and
[its tracker](2026-08-08-v1.1.0-audit-campaign-tracker.md) (per-issue evidence,
chunks 0-22). This program supersedes the campaign's *disposition* bar with a
**fix** bar, for the reason in [Why this replaces the disposition bar](#why-this-replaces-the-disposition-bar).

---

## Global Constraints

Copied verbatim; every phase's requirements implicitly include these.

- **Backward compatibility is NOT a constraint.** There are essentially no users.
  Do not weight "this would break existing users, baselines, pinned tags, or
  `history.db` rows" in any decision. The constraints are **time and evidence**.
- **Pre-commit order:** Black runs before Ruff. `make fmt && make lint && make test`
  before every commit. (`make` is **not on PATH** on this machine — invoke the
  underlying commands or `pre-commit run`.)
- **No `shell=True`** in subprocess calls, ever.
- **Conventional commits**, no trailers, no AI-attribution markers.
- **Never commit directly to `dev`.** Branch, PR into `dev`, squash.
- **Closing keywords go in an individual COMMIT message.** On a PR into `dev`
  the PR body is inert — not "linked but not yet closing", *inert*. Measured and
  corrected 2026-08-26; full evidence in
  [Closing keywords: what actually fires](#closing-keywords-what-actually-fires).
  A bare `#N` is still only a cross-link. Build any list mechanically and diff it
  case-insensitively — a `grep -oE 'closes #'` misses a capitalised
  `Closes #538`.
- **CI's only enforced coverage floor is 85%** (`coverage-aggregate`'s "Verify
  coverage threshold" step) — raised from 80 in Phase 1 (#756) after measuring
  86.87% on the marker-filtered suite. Cite the job and step, never a
  line number: that citation read `ci.yml:734`, `:788` and `:816`, and was stale
  at two of the three. Nothing sets `--cov-fail-under`, so there is still no
  local gate.
- **Run local suites with `PYTHONUTF8` unset.** CI pins it to `1`, an environment
  no real Windows user has; setting it hides the entire console-encoding class.
- **Bound local runs:** `JMO_THREADS=2 .venv/Scripts/python.exe -m pytest tests/ -n 8 -q
  -m "not smoke and not requires_tools and not docker"`. Unbounded `-n auto`
  manufactures failures (#767).
- **Read the `windows-2022` job log, never its check tick** — it is
  `continue-on-error: true` and reports green over exit code 1. Diff the **skip**
  count as well as passes.
- **Check both CI events.** Every push runs `ci.yml` twice (`push` and
  `pull_request`) and they see different worlds. A flake fails one; a regression
  fails both.
- **Mutation-test every guard.** A guard that survives its own defect must be
  fixed or deleted. Give each a negative control, and restore from a **file
  backup**, never `git checkout --`.

**Baselines to beat, measured 2026-08-22 at `5d8beed`** (re-measure, don't quote):

| Measurement | Value |
|---|---|
| collection, CI floor filter | 9047 / 9277, 230 deselected |
| local run, campaign filter, in halves | 8939 passed, 106 skipped, 2 failed, 1 error |
| `windows-2022` **job log**, both events | 8930 passed, 118 skipped, 230 deselected |
| `jmo validate --tier full` | GO, 278/290 PASS, 4 WARN, 8 SKIP |

The 2 failures and 1 error in the local run are **#967** and **#968**, and they
are fixed in Phase 0 — so Phase 0's own acceptance is that this line reads
`0 failed, 0 error`.

---

## Why this replaces the disposition bar

The campaign's release bar was deliberately a **disposition** bar: *"requiring 40
fixes before a release is how a campaign stops shipping."* That reasoning assumed
fixes were expensive and shipping was urgent.

Neither holds. There are essentially no users, and v1.1.0 is effectively the
first release — so there is nobody waiting, and "breaks existing behaviour" is
not a cost. Deferring 93 issues to protect a release nobody is waiting for
optimises for the wrong thing. **The disposition labels are not wasted**: they
are the queryable input to this program, and they remain the historical record of
what chunk 22 decided and why.

## Why there are no task-level steps here

`CLAUDE.md`: *"A plan written without running anything is a set of predictions,
not a spec. The #694 encoding plan had 8 measured-false claims, including a
reproduction that could never fail and a scoping premise that turned out nearly
disjoint from the actual failures (fixing all 153 fixed 0)."*

Expanding 80 issues into TDD tasks with concrete code, now, would manufacture
several hundred unmeasured predictions across subsystems this session has not
read. Each phase below therefore carries a **Measure first** block naming what to
establish before its detailed plan is written. That is the same shape the audit
campaign used — a strategy doc plus a tracker, with per-chunk detail added when
the chunk was reached — and it worked for 23 chunks.

---

## Ordering constraints (the load-bearing ones)

These are not preferences. Violating them wastes work:

1. **Phase 0 precedes everything.** You are about to make ~80 changes and verify
   them with a suite that currently drops 21 tests silently (#968), a `make lint`
   that cannot fail (#890), coverage that under-reports (#767), and tests that
   mutate your real `~/.jmo/config.yml` and `.jmo/history.db` (#933, #802).
   Fixing instruments before taking 80 measurements is what makes the
   measurements mean anything.
2. **#808 precedes #861.** #808 deletes six dead discovery helpers and rehomes
   **129 tests** written against dead code. Doing #861 first means migrating
   those 129 tests through a path-normalisation change for no reason.
3. **#861 precedes Phases 5, 6 and 7.** `location.path` normalisation changes
   `common_finding.fingerprint()`, which every downstream artifact, diff baseline
   and dashboard render keys on. Anything fixed downstream first gets
   re-validated.
4. **Phase 7 (dashboard) follows Phase 4.** The dashboard renders
   `location.path`; building it before normalisation ships a dashboard that
   displays four spellings of one file.
5. **Phase 8 (docs) is last.** Documentation describes behaviour. Write it once
   behaviour is final.

---

## Phase 0 — Instruments: make the suite incapable of lying

**17 issues:**
`#968 #890 #767 #802 #933 #907 #834 #858 #921 #723 #873 #792 #800 #967 #930 #916 #973`

> **Rosters are parsed, not read.** `scripts/dev/phase_audit.py` derives the
> phase labels and the unclaimed-issue query from these lines, so each one is
> the *as-executed* set, not the as-planned one. #973 was filed during the phase
> and fixed in it (see below), so it belongs here; it was previously named only
> in prose, which left it out of every mechanical check. Keep a roster on one
> line — markdownlint MD018 reads a wrapped continuation beginning `#` as a
> heading.

**Why first.** Every one of these makes a signal you are about to rely on
untrustworthy, or damages the environment you are working in:

- **The suite silently drops tests.** #968 — a bare `from conftest import` races
  under `-n 8`; a worker that collected `tests/jmo_mcp/` first binds `conftest`
  to that file and **21 tests do not run**, reported as a collection *error*, not
  a failure.
- **The suite damages your machine.** #802 writes real scans into
  `.jmo/history.db` (+38 scans, +4240 findings in one session, measured); #933
  increments `scan_count` in your real `~/.jmo/config.yml`; #907 shells out to a
  real networked `semgrep --config auto` with no `requires_tools` marker.
- **Gates that cannot fail.** #890 (`make lint` is `|| true`), #834, #858, #921 —
  guards that survive their own defect. #723 — a `timings.json` contract test so
  vacuous it is *why* the schema drift survived.
- **Tests that do not test.** #930 (60 schedule CLI tests use `MagicMock` args,
  never the real parser) and #916 (52 trend `Args` stubs, 0 real-parser tests).
  You are about to change the CLI in Phases 3-6; these 112 tests would not catch
  a regression.
- **Flakes that will be blamed on your changes.** #873, #792 (1% headroom on a
  500 MB absolute RSS delta), #800 (0.068% measured).
- **#967** belongs here, not with the product bugs: `mark_resolved` writes an
  expiry in UTC and `suppress.py` evaluates it in local time, so its two tests
  are **green in CI by construction** — runners are UTC. A test that cannot fail
  where it runs is an instrument defect.

**Measure first:** run the suite in halves with `PYTHONUTF8` unset and capture
the failing-test **ID set**, not the count — a change can fix N and break N. Snapshot
`.jmo/history.db` and `~/.jmo/config.yml` before starting; `-20260808` is the only
pre-migration snapshot and must never be overwritten.

### Measured baseline, 2026-08-23 at `3c55886` (supersedes the `5d8beed` table above)

Halves used for every Phase 0 measurement:
**A** = `tests/adapters tests/cli tests/core`;
**B** = `tests/unit tests/reporters tests/jmo_mcp tests/attestation tests/performance
tests/security tests/e2e tests/integration tests/cross_platform tests/edge_cases`.
Runner: `PYTHONUTF8` unset, `JMO_THREADS=2`, `-n 8`, `-p no:randomly`, `--timeout=300`,
`--junitxml`, campaign marker filter.

| Half | junit testcases | passed | skipped | failed | error | wall |
|---|---|---|---|---|---|---|
| A | 4468 | 4462 | 6 | 0 | 0 | 297 s |
| B | 4600 | 4497 | 102 | 1 | 0 | 188 s |

Real tests: A 4468 + B 4599 = **9067**, exactly single-process collection. B's 4600th
junit entry is a **synthetic collection-skip record** for
`tests/e2e/test_dashboard_visual` (playwright absent), which `--collect-only` does not
count. That accounts for the whole difference; no test is dropped.

With half B's one flake passing (it does, intermittently), the line reads
**8960 passed, 108 skipped, 0 failed, 0 error** — matching the figure #967 itself quotes.

**Two defects are absent from this baseline and that is expected, not evidence:**

- **#968** needs `tests/core` and `tests/jmo_mcp` in the *same* `-n 8` invocation; the
  A/B split separates them. Reproduced separately and **deterministically**:
  `pytest tests/core tests/jmo_mcp -n 8` → `887 passed, 2 skipped, 1 error`.
- **#967** can only fail while the local and UTC dates differ. Measured at run time:
  local `2026-08-23 00:30`, UTC `2026-08-23 04:30` — dates agree, so both its tests
  passed. Its window here is ~20:00–23:59 EDT. **Verify by patching the clock, never by
  running the suite and observing green.**

### Acceptance — each line is a command with numbers, not a claim

1. **`0 failed, 0 error`** on both halves, `PYTHONUTF8` unset, `-n 8`.
2. **The post-fix node-ID set differs from the baseline set only by intended additions.**
   Diff `--junitxml` against `--junitxml` (set vs set), never count vs count, and never
   junit vs `--collect-only` — the latter mis-parses parametrised IDs containing
   newlines or backslashes (35 false positives measured).
3. **A full run mutates nothing outside `tmp_path`**: `~/.jmo/config.yml` byte-identical,
   and `.jmo/history.db` `scans`/`findings` row counts unchanged, before vs after.
   Pre-fix reference: `scan_count` 1019 → 1022 during baseline alone; DB at 2410 scans /
   204500 findings.
4. **`make lint` can fail** — demonstrated by a real ruff violation producing a non-zero
   exit, with the old recipe shown exiting 0 on the same violation.
5. **`-n auto` and `-n 8` report the same coverage**, with no "failed to return coverage
   data" message in either.
6. **Every guard touched here has been mutated and observed to fail.** A guard that
   survives its own defect is fixed or deleted — no exceptions, and the red is pasted in
   the task report.

### Surfaced while establishing the above

- **#973 (filed)** — `test_policy_evaluation_performance_large` asserts an OPA
  subprocess finishes in <500 ms; measured **1608 ms**, 3.2× over, on the same tree that
  passed minutes earlier. It `skipif`s on `opa_available()`, and CI installs no `opa`
  (#922), so it can only ever redden a local run. Folded into Phase 0 because acceptance
  line 1 is unachievable while it flips at random. Same family as #792/#873/#800.
- **`scripts/core/report_orchestrator.py` does not exist.** #802 and #723 both cite it;
  the file is `scripts/cli/report_orchestrator.py` (history-db default at `:468-470`,
  timings producer at `:422-430`).
- **#723 is partly fixed already** — `test_timings_data_structure` now asserts
  `aggregate_seconds`/`jobs`/`meta`. What remains is that the whole file is
  `requires_tools`+`slow`, so PR CI evaluates none of it.
- **#890 undercounts**: `make lint` has **five** `|| true`, including
  `pre-commit run --all-files || true`.
- **#873 undercounts**: a class-level `@pytest.mark.timeout(180)` sits above the 120 s
  subprocess cap, so raising only the subprocess cap cannot work.
- **#916 count drift**: 24 `branch = "main"` fixtures, not 28. Its substantive claim
  holds — **0 of 2410** live scans have `branch='main'`.

### Phase 0 outcome, measured at `13505e1`

All six acceptance criteria met. Numbers below are from commands, not estimates.

| # | Criterion | Result |
|---|---|---|
| 1 | `0 failed, 0 error` on both halves | A **4474 passed / 6 skipped**, B **4532 / 100**, `rc=0` both |
| 2 | post-fix node-ID set differs only by intended additions | A: **+12 / −0**, newly-red **0**. B: **+33 / −1**, newly-red **0**. The single removal is the intended rename `test_100k_findings_memory_usage` → `test_30k_findings_memory_usage` |
| 3 | a full run mutates nothing outside `tmp_path` | controlled before/after around **both** halves: config SHA-256 identical, DB `scans`/`findings` identical |
| 4 | `make lint` can fail | old recipe exit **0** on a planted `F401`; new recipe **non-zero** on the same violation; clean tree **0** |
| 5 | `-n auto` and `-n 8` report the same coverage | both **84%** (3113 vs 3114 missed of 22681); **zero** "failed to return coverage data" in either — was a void **13%** with worker `gw12` dead |
| 6 | every guard mutated and observed to fail | negative controls pasted in all 11 task reports |

Two half-B deltas, both adjudicated as non-regressions: `test_policy_evaluation_performance_large`
went failure→passed (Task 6's #973 fix working), and `test_benchmark_6_memory_usage_10k_findings`
went skipped→passed (the mid-session psutil install, #977 — an environment change).

### What the phase actually repaired

- **The suite no longer writes to real user state.** 25 writer tests were found by the guard
  itself and fixed. Before: `scan_count` climbed ~+50 per full pass, and `.jmo/history.db`
  gained ~13 scans per run.
- **21 tests that were silently dropped now run** (#968, a bare `conftest` import racing under
  `-n 8`, reported as a collection *error* rather than a failure).
- **Coverage under `-n auto` is no longer void** — 13% → 84%, matching `-n 8`.
- **`make lint` can fail** — and is now scoped to exactly what CI blocks on, no wider.
- **A user-reachable security bug fixed** (#967): suppression expiries were written in UTC and
  evaluated in local time, so a security suppression's lifetime was off by up to a day.

### Five issues filed during the phase

| Issue | What |
|---|---|
| **#973** | `test_policy_evaluation_performance_large` — absolute 500 ms OPA gate, measured 1608 ms. Folded into Phase 0 and fixed |
| **#974** | `jmo trends developers` reads `--repo`/`--team-file` that **no parser flag declares** — found by the new guard on its first run |
| **#975** | `jmo.yml`'s `profiles.<name>.tools:` is **dead config** — `_effective_scan_settings()` never reads it, so a custom narrow profile silently runs the full built-in one |
| **#976** | Three residual gaps on the `requires_tools` surface, which has no routine exerciser |
| **#977** | `psutil` is **undeclared** — the memory tests skip everywhere, and **#792's "runs at PR time" premise is false** |

### Two things worth your decision

1. **Four open issues appear already fixed on `dev`** — #781, #779, #912, #910. Task 11
   measured each live: all 15 `jmo trends` invocations `rc=0`, `developers` no longer
   `KeyError`s, `--export-html` produced valid 7126-byte HTML. I did not close them; that's
   your call, and it would take four issues off the 80-issue program.
2. **#977 needs a maintainer decision** — declare `psutil` in `[dependency-groups] dev` so
   the memory gates actually run, or mark those tests as deliberately out of CI.

## Phase 1 — CI gates that actually gate

**7 issues:** #960 #962 #961 #922 #756 #976 #977

Both #976 and #977 were filed during Phase 0 and **fixed in Phase 1**, but named
only in Phase 0's "filed during the phase" table — so no roster carried them and
no mechanical check could see them. They are two of the eleven issues #1000 is
about. Added to the roster 2026-08-26.

**Why here.** Phase 0 fixes the suite; this fixes whether CI *enforces* it.
Branch protection requires exactly **one** check (`quick-checks`) and only on
`main` (#960), so almost every job is advisory. 60 Docker tests are run by no CI
job and no local marker filter (#962). #961's assertion tests a unit the daemon's
image store decides — two units 4.4× apart. #922: CI installs no `opa`, so every
real-policy test skips there. #756: the 85% floor is enforced nowhere.

**Measure first:** for each job, determine whether it is `continue-on-error`,
whether it is in the required-checks set, and whether it runs on `push`,
`pull_request` or both. "Blocking" has meant two different things across this
campaign — read #960 before calling anything a gate.

**Acceptance:** every job that can fail the build is named, and every job that
*cannot* is either made blocking or documented as advisory on purpose.

## Phase 2 — Delete before refactor

**4 issues:** #944 #808 #765 #796

**Why here.** Every line deleted now is a line the next six phases do not have to
migrate, retest, or reason about. **#808 is the load-bearing one** — six dead
`_iter_*` helpers with zero production callers. The live path is
`ScanOrchestrator._discover_*`, and the two implementations have diverged in
*both* directions; #803 and #807 existed precisely because a fix landed only in
the copy that stopped being called. Also: `BaseScanner`/`ScanResult` have zero
production subclasses.

> **MEASURED CORRECTION 2026-08-26.** This said #808 carried **129 tests**, taken
> from the issue. 129 is a count of grep *occurrences*; an AST pass finds **53
> test functions** holding them. Deleted in total: 53 + 8 (`base_scanner`) + 18
> (#944) = **79**, independently confirmed by pruning `.test_durations`, which
> removed exactly 79 node ids. Collection went 9281 → 9204 (−79 +2 re-homed).

Also in this phase: #944 (SigstoreSigner's OIDC subsystem, ~100 lines and ~20
tests, no callers), #765 (unreferenced duplicate schema), and #796
(gitleaks/tfsec ghost references in stubs, membership lists and shell scripts).

**Measure first:** prove zero production callers for each — `graphify` or
`find_referencing_symbols`, not grep alone, because grep misses dynamic dispatch.
Chunk 22's `dead code audit lessons` memory records the blind spots.

**Acceptance:** collection count drops by the number of deleted tests and by no
more; no import of a deleted symbol survives; the suite is green.

> **Carve-out recorded 2026-08-26.** #796's ghost sweep deliberately left
> `rule_equivalence.py`'s six `gitleaks` tuples alone. They are unreachable
> `_REVERSE_MAP` keys and removing them is behaviour-neutral, but
> `RULE_EQUIVALENCE` is owned by **#846 in Phase 4**, which will rewrite that
> structure — so Phase 4 should expect to find them and remove them there.
> #796's own line citations had already rotted (`scan_utils.py:263` is now
> `:422`), so its sites were re-measured rather than followed, and the sweep came
> back **larger** than filed: `basic_reporter.py` carried the same two membership
> lists the issue named only in `compliance_mapper.py`.

## Phase 3 — One-liners and single-site fixes

**17 issues:** #950 #958 #875 #929 #927 #789 #908 #931 #790 #926 #859 #791 #788 #795 **#974 #980 #985**

The last three were folded in 2026-08-26 from the unscheduled set (#996):

- **#980** — `test_scan_each_tool_happy_paths` fails on Windows because its
  `shutil.which` mock returns fabricated POSIX paths a newer existence check
  rejects. Red for any Windows developer running the full suite.
- **#985** — the nightly's **Integration Tests** job fails **every night**. Two
  independent causes, measured 2026-08-26: (a) `actionlint` SC2129 in
  `scheduled.yml` — **already fixed on `dev`, resolves at the next sync, no work
  here**; (b) six tests in `tests/integration/test_scan_pipeline.py` write the
  real `~/.jmo/history.db` and `~/.jmo/config.yml`. Phase 1's writer sweep
  (`8fc6659`) touched only `test_wizard_tool_checker.py` and
  `test_cli_profiles.py`, so this file was never fixed. It is
  `@pytest.mark.requires_tools`, so **PR-time CI never runs it** — only the
  nightly does. Same blind spot as #976.
- **#974** — `cmd_trends_developers` reads `args.repo` and `args.team_file`,
  which no parser flag declares. **The fix direction is an open fork** (add the
  two flags, or delete the dead reads and the team-aggregation branch they
  gate); maintainer decided 2026-08-26 to settle it when Phase 3 reaches it,
  after the team branch has been read in full.

**Why here.** Cheap, independent, and each is a defect a user hits. Banking them
early shakes out the branch/PR/verify workflow at low risk before Phase 4's
cross-cutting work. Several are documented one-liners: #950 (an uppercase hex
digest reports `TAMPER DETECTED` on an unmodified subject), #958
(`jmo mcp-server` calls `input()` at `jmo.py:3850` and eats the client's first
JSON-RPC message).

**Measure first:** for each, reproduce the defect and capture the actual output
*before* fixing. A finding without a reproduction is a hypothesis — and #694's
plan contained a reproduction that exited at argparse before reaching the bug.

**Acceptance:** each has a regression test that fails before the fix and passes
after, demonstrated by mutation.

## Phase 4 — The data path, upstream-first

**6 issues, in this order:** #861 → #837 → #848 → #845 → #846 → #849

**This is the phase the whole ordering exists to protect.** The order mirrors
`normalize_and_report.py:gather_results`.

- **#861 — `location.path` is unnormalised.** One `deep` scan of one directory
  produced **four spellings of the same file**: 149 of 242 paths contain a
  backslash, 64 carry a leading separator, 66 are absolute. Against a ground
  truth of 77 findings in one file, dedup is measurably wrong — and **the host's
  absolute path ships in SARIF and the dashboard**. This was deferred as a
  migration only because it changes `fingerprint()` and invalidates baselines;
  with no users there are no baselines to protect.
- **#837 — every adapter discards the tool's own error channel**, so a file a
  scanner *could not analyse* is reported as clean. **0 of 8** adapters read it.
  For a security tool this is the most consequential defect in the backlog: a
  false negative that looks like a pass.
- **#848** Phase-1 dedup silently discards any finding with a falsy `id`.
- **#845** CWEs present in tool output never reach `risk.cwe`, starving every
  compliance mapping downstream.
- **#846** `RULE_EQUIVALENCE` declares genuinely different controls equivalent.
- **#849** EPSS makes one HTTP request per finding with an unknown CVE and builds
  an unbounded bulk URL.

**Measure first:** capture a real `deep` scan's findings **before** any change —
path spellings, fingerprints, dedup counts, `risk.cwe` fill rate — as the
comparison corpus. Note that ~67% of `history.db` is test/dogfood noise, so any
assertion built on it measures noise unless filtered.

**Acceptance:** one file yields one path spelling; no host absolute path appears
in any exported artifact; a file a tool failed to read is reported as an error,
not as clean; dedup counts move in the measured direction and the movement is
explained.

## Phase 5 — Command surface

**13 issues:** #801 #825 #824 #787 #870 #901 #894 #879 #867 #963 #936 **#975 #835**

**#835** scheduled here 2026-08-26, and it is the same thesis as #825. `scancode`
is invoked with **no detection flag** — no `--license`, `--copyright`,
`--package` — so it walks the tree for up to its 1200 s timeout, writes 30,496
entries carrying only `path`/`type`/`scan_errors`, and its adapter reads
`license_detections` and `copyrights`, which cannot exist in output produced that
way. Zero findings are structurally possible, and the tool is graded `success`.
It is in the `deep` profile. Its `disposition:BLOCKED-UNVERIFIABLE` label refers
to confirming the *fix* without scancode installed, not to the defect, which is
measured in the issue from a real recorded run.

**#975** folded in 2026-08-26 from the unscheduled set, and it is this phase's
thesis exactly: `jmo.yml`'s `profiles.<name>.tools` is **never read** —
`_effective_scan_settings()` resolves from the built-in `PROFILE_TOOLS` — so a
user who writes a narrower custom profile silently gets the full built-in one, a
longer and more network-dependent scan than they asked for, with no warning that
their configuration was ignored. A documented, user-facing key that does nothing.

Must follow Phase 4 — most of these read or write findings. Grouped: silent
success (#801 the auto-storage hook swallows every exception so a scan
"succeeds" having stored nothing; #825 `--allow-missing-tools` records a tool
that never ran as a success), history correctness (#787 records the config's tool
list rather than what ran; #870 `jmo fast|balanced|full` store nothing; #901
aborts the whole scan on two findings without an `id`; #894 every read rewrites
the DB header to WAL), output control (#879 `--log-level` missing from four
subcommands; #867 `outputs: []` still writes four files; #824 gitlab never writes
`scan-timings.json`), and Windows (#963 fifty-six `subprocess.run(text=True)`
sites decode as cp1252, one crashing `jmo build validate`; #936 seven more in
`update_versions.py`).

**Measure first:** #963's 56 sites — confirm the count and that the lint rule is
a **lower bound**. `ruff --select PLW1514` flags 153 sites against 1198 real ones
and cannot type `(tmp_path / "x").read_text()`, the dominant pytest idiom.

**Acceptance:** no command reports success over work it did not do.

## Phase 6 — Subsystems

**9 issues:** schedule (#928 #934) · MCP (#952 #953) · attest (#945 #946) ·
build (#935 #939) · instruments (**#994**)

**#994** folded in 2026-08-26 from the unscheduled set: the scanner-spawn
recorder is scoped to semgrep only, so "no guard fired" was read as "nothing
spawned" while six tests performed a real `uv pip install` into the developer's
venv. Widening it belongs with the other subsystem work — it is independent and
parallelisable like the rest.

Independent of each other; parallelisable. #934 — one unknown key in
`schedules.json` makes *every* schedule unreadable, surfaced as a bare
`TypeError`. #939 — `jmo build`'s pre-build version gate **fails open** on three
error paths, all returning "validation passed".

**Measure first:** #935's version validation needs `versions.yaml` and the
Dockerfiles read together; chunk 21 established `--sync` had been reporting
success over an 18-version-stale pin.

## Phase 7 — Dashboard

**2 issues:** #862 #864

**Must follow Phase 4** — the dashboard renders `location.path`.

A released (pip-installed) JMo **cannot build the React dashboard at all**: the
wheel carries 190 entries and **0** under `scripts/dashboard/`. It falls back to
a vendored build dated **2025-11-17**, which is also what CI renders — so CI has
been validating a fixture, not the product.

**Measure first:** `uv build` and list the wheel contents. Decide whether the fix
is packaging the built assets or building at install time; that decision changes
the whole phase.

**Acceptance:** a wheel installed into a clean venv produces a dashboard built
from current sources, and CI renders that same artifact.

## Phase 8 — Claims and documentation

**7 issues:** #740 #761 #923 #855 #750 #742 #758

Last, because documentation describes behaviour and behaviour is now final.
Take #855 first — the documented "30-40% noise reduction" measures **~1%** on
both available corpora. Then #923, where the `hipaa-compliance` docs describe a NIST CSF gate while the
policy enforces CWEs and 164.312 safeguards. #750 — the doc version header guard
(the header value itself was corrected at `92650ec`; the guard is what stops it
recurring). #742 and #758 sit here rather than Phase 0 because they are test
*quality*, not test *integrity* — they do not make other work untrustworthy.

**Acceptance:** every measured claim in the docs either matches a number this
program produced, or is removed.

## Phase 9 — Release preparation

**9 issues:** #631 #704 #705 #706 #390 #900 #964 **#993 #1000**

**#1000 — eleven issues are fixed on `dev` and would not close at the sync.**
Scheduled here 2026-08-26 because it is a property of the sync, and its
verification can only run at the sync. The measurement, and the correction it
forces, are in
[Closing keywords: what actually fires](#closing-keywords-what-actually-fires).
The mechanism landed in Phase 2.5; **what remains for this phase is to run the
verification and confirm all eleven close.** Do not treat it as done before then
— the whole finding is that the bookkeeping looked done and was not.

**#993** folded in 2026-08-26 from the unscheduled set, and it is a **maintainer
decision about repo settings, not code** — so it belongs exactly here, at the
sync it governs. Two hazards that recur on *every* sync: squash-only merging
guarantees `main` never becomes an ancestor of `dev` (so `/done`'s Phase A stops
on a condition that is correct by design), and `dev` is deletable by its own sync
PR under `delete_branch_on_merge`. Decide both before running the final sync, not
during it.

Dependency bumps deliberately last: a linter or formatter bump can carry a new
ruleset that reflows the codebase or reddens CI on unchanged files, and a tool
bump can change an output schema an adapter parses. Neither is something to
discover between the last fix and the tag.

**#900 first within this phase** — exact-version npm `overrides` pins are the
reason the *next* CVE cannot be fixed, and there is an open **HIGH** Dependabot
alert (#156, `js-yaml`, quadratic CPU consumption in `!!omap`) that may be
blocked by exactly that.

**#964 — delete `:latest-deep`, `:latest-slim` and `:full` from GHCR.** The docs
already say these do not exist, so deleting makes them true about fact as well as
intent and needs no doc edit. Requires
`gh auth refresh -h github.com -s read:packages,write:packages`. **Do not
bulk-delete untagged versions** — they are the per-architecture children of the
multi-arch manifest lists, and deleting them breaks the tagged manifests that
point at them.

Then **Phase 10 runs**, and only then: CHANGELOG (from the per-chunk
user-visible sections, including chunk 9's `### Security` entry), version bump in
`jmo.py` / `pyproject.toml` / `docs/CLI_REFERENCE.md` **together**, `uv lock`
(the lock pins the project's own version, so a bump alone makes it stale),
`dev → main`, tag.

## Phase 10 — The vacuous-assertion sweep

**1 issue:** #979

**Scheduled 2026-08-26**, having been shown the cheaper option and its cost. The
maintainer chose to run it rather than defer, on the same reasoning as Path C:
the sweep is what keeps the *rest* of the program's verification honest.

**Why last.** 103 of 202 hits need a per-hit judgement, and roughly half the
matches are legitimate preconditions — a blanket codemod would destroy 99 working
assertions to fix 103 dead ones. That is a session of careful, wide-surface test
editing, which is the last thing that should run before the data-path phases have
settled. Running it after Phase 9's issue work, but **before** the CHANGELOG /
version-bump / sync / tag sequence, keeps the tag last without letting a
suite-wide edit destabilise Phases 4-7.

**This is not hypothetical bookkeeping.** Phase 2 hit the same class twice in one
session: two CRLF tests written that day survived every mutation (`read_text()`
strips CR during the read, so they asserted a CPython guarantee), and
`test_get_category_summary_fallback_inference` had asserted a category for
`gitleaks` via a ruleId containing `"key"` — so it could not fail, and outlived
the tool by three releases.

**Measure first:** the per-hit V/F inventory lives in the Phase 0 workspace
report for task 9. Transcribe it into #979 before that workspace is deleted, or
the sweep starts by re-deriving 202 judgements.

**Acceptance:** every assertion changed is mutation-proven, or deleted. An
assertion rewritten into a different vacuous shape is not progress.

---

## After the tag

**21 issues, deliberately not phase work.** ROADMAP features
`#34 #37 #38 #48 #49 #52 #53 #54 #55`; enhancements #722 (per-tool scan
instrumentation), #770 (an occurrences model instead of deduping), #943 and #947
(attestation policy design); deferred debt `#972 #981 #998 #999 #1003`; trackers
`#445 #715 #996`.

The ROADMAP nine and the four enhancements are **features, not defects**. They
were dispositioned `DEFER-POST-1.1.0` once, as a set, in chunk 22.

**The other seven were added 2026-08-26, and the heading is now "not phase work"
rather than "features only" because that is what the list holds.** It is the
plan's only exit that is not a phase, so anything genuinely out of scope has to
land here or the unclaimed query stays red — which is the point. Each carries a
reason:

| Issue | Why it is not phase work |
|---|---|
| #972 | no harness executes `scheduled.yml`'s `github-script` bodies. Test infrastructure the program does not depend on; `disposition:DEFER-POST-1.1.0` since chunk 22 |
| #981 | `scans.duration_seconds` is NULL on 100% of rows. **A user-reachable defect**, deferred by its existing chunk-22 disposition rather than by a decision taken here — flagged for the maintainer, not settled |
| #998 | `jmo-refactoring-assistant`'s worked example teaches the `_iter_*` extraction Phase 2 deleted. Documentation of a maintainer-local skill |
| #999 | `generate_comparison_report.sh`, 194 lines, zero callers. **Deleting it is a bigger call than #796 made**, so it waits for one |
| #1003 | the tool CATALOGUE total is stated as both 28 and 29 across ~25 current-state doc claims, unguarded. Filed in Phase 3 while fixing #795's cascade and deliberately not absorbed there: the deep-PROFILE counts moved 28 -> 29 in that change and were fixed with it, but these were already wrong beforehand — `README.md` says 28 on line 13 and 29 on line 83. A docs sweep plus a guard, not a defect in the product |
| #996 | the tracking issue for the seven unscheduled issues. Closes derivatively once they do; it is not itself work |

**#981 and #999 are the two worth a second look.** Both were dispositioned when
the bar was "ship with it"; the bar is now PATH C. They are parked here because
honouring an existing disposition is not a new decision, and re-opening one
inside a bookkeeping session would be. Move either into a phase if that is the
call.

---

## Closing keywords: what actually fires

Three sessions wrote `Closes #N` in a PR body targeting `dev` and closed
nothing. Two documents then disagreed about why, and **both were wrong**. What
follows is measured, 2026-08-26.

| PR | base | `closingIssuesReferences` | keywords in its squash commit |
|---|---|---:|---:|
| #978 (Phase 0) | `dev` | **0** | 17 |
| #995 (Phase 1) | `dev` | **0** | **0** |
| #997 (Phase 2) | `dev` | **0** | **0** |
| #986 (the last sync) | `main` | **86** | 93 |

**GitHub creates closing references only for a PR whose base is the default
branch.** A `Closes #N` in a PR body aimed at `dev` is not "a link that fires
later" — GitHub never records it at all. That is why all three phase PRs report
zero, and why a body-only keyword is unrecoverable once the branch is deleted.

So the two standing explanations were each half right and, combined, misleading:

- *"Keywords go in the PR body; a PR into `dev` links but does not close"* —
  **false in its load-bearing half.** There is no link. #986 worked because its
  base was `main`, not because bodies work.
- *"A squash merge drops the PR body"* (#1000) — **true, but not the mechanism.**
  The body was already inert. And a squash does *not* drop commit messages: it
  concatenates them, which is exactly what makes the commit path work.

**The path that survives, measured end to end:** `b92b8cf` (Phase 0's squash into
`dev`) carries 17 keywords because its branch's individual commits carried them;
the `dev → main` squash `3fb64fb` then carries **93** in a 246 KB, 4,665-line
message, GitHub accepted it, and all 17 Phase 0 issues are CLOSED. A keyword in a
commit message survives two consecutive squashes.

**Rule: put `Closes #N` in an individual commit message.** The PR body may repeat
it for readers; it is not what fires. The sync PR's body is a *belt* — useful,
because its base is `main` so it does work there — but the commit is the braces,
and it is written by the session that knows.

### The eleven

Fixed on `dev`, no keyword anywhere in its history, so they will not close:

```text
#960 #962 #961 #922 #756 #976 #977   (Phase 1, PR #995)
#765 #944 #808 #796                  (Phase 2, PR #997)
```

Their history cannot be rewritten (`dev` is never force-pushed), so the keywords
are carried by a **bookkeeping commit** on `dev` instead — same mechanism,
applied after the fact. Verify it survived, and that the survivors are exactly
the set expected:

```bash
python scripts/dev/phase_audit.py closers --check
```

**Why the old acceptance query was not enough.** It read:

```bash
git log origin/main..origin/dev --format='%B' \
  | grep -oiE '(close[sd]?|fixe[sd]?|resolve[sd]?)[[:space:]]*#[0-9]+' \
  | grep -oE '#[0-9]+' | sort -u | wc -l
```

Run at `4281ae7` it returns **91** — and is wrong in both directions. It counts
Phase 0's 17 issues, which are already closed on `main`, and it misses all eleven
above. A bare count cannot tell you either of those things; `closers --check`
compares the keyword set against the open issues the plan says are fixed.

## Tracking

The disposition labels stay as chunk 22's historical record. **They are not a
schedule** — 84 of the 95 open issues carry one, including every issue #996 was
filed about. Scheduling lives in the phase rosters above, and the `phase:N`
labels are derived from them:

```bash
python scripts/dev/phase_audit.py derive          # the mapping, no network
python scripts/dev/phase_audit.py labels --apply  # push it to GitHub
gh issue list --state open --label "phase:3" --json number --jq 'length'
```

**Run this at the start of every remaining phase, and it is wired into CI so
forgetting is not fatal:**

```bash
python scripts/dev/phase_audit.py verify
```

`verify` fails on two things: an open issue the plan names nowhere, and any
disagreement between the rosters and the `phase:N` labels **in either
direction** — a missing label, or a label the plan does not justify. The second
half matters because `gh issue list --label phase:3` is only trustworthy while
the labels still match the document nobody re-read.

**Acceptance for the program:** every issue in Phases 0-10 is closed by a
closing keyword that reached `main`, not by hand and not by a bare `#N`. Verify
before tagging with `phase_audit.py closers --check`, not with a raw count — see
the section above for why a count is not enough.

## Phase summary

| Phase | n | Gate it protects | State |
|---|---:|---|---|
| 0 — instruments | 17 | every later measurement | **DONE** — PR #978, merged `b92b8cf` |
| 1 — CI gates | 7 | whether CI enforces phase 0 | **DONE** — PR #995, merged `1d8a916` |
| 2 — delete | 4 | surface area of phases 3-8 | **DONE** — PR #997, merged `4281ae7` |
| 3 — one-liners | 17 | — | +#974 #980 #985 |
| 4 — data path | 6 | phases 5, 6, 7 | ordered; do not run beside another phase |
| 5 — command surface | 13 | — | +#975 #835 |
| 6 — subsystems | 9 | — | +#994 |
| 7 — dashboard | 2 | — | |
| 8 — docs | 7 | — | |
| 9 — release prep | 9 | the tag | +#993 #1000 |
| 10 — vacuous assertions | 1 | the honesty of every other phase's gates | scheduled 2026-08-26 |
| **before tag** | **92** | | |
| after tag | 21 | not phase work | |

> **These counts are now AS EXECUTED, and this table is checked, not trusted.**
> `scripts/dev/phase_audit.py derive` asserts each `n` above against the
> corresponding phase roster, each roster against its own `**N issues:**`
> prefix, and the total against the sum — and `tests/unit/test_phase_audit.py`
> runs that assertion in CI. Editing a roster without editing this table now
> reddens the build.
>
> The previous revision said the opposite: *"The counts here are **as planned**;
> the as-executed figures are in each phase's handoff."* That was a deliberate
> choice, and it is what left #973, #976 and #977 in no roster — three issues
> fixed months apart that no mechanical check could see, two of which are in
> #1000's eleven. **A document that is parsed cannot also be aspirational.** The
> as-planned figures were 16 (Phase 0) and 5 (Phase 1); they are recorded here
> and nowhere else, because nothing needs to query them.

### Scheduled 2026-08-26 from #996's unscheduled set

Seven open issues had neither a `disposition:` label nor a phase, so nothing
would have caught them at the tag. All seven now have a home:

| Issue | Went to | Why |
|---|---|---|
| #974 | Phase 3 | single-site; fix direction deliberately left open until Phase 3 reads the team-aggregation branch |
| #975 | Phase 5 | a documented user-facing config key that does nothing — this phase's thesis |
| #979 | **Phase 10** | one session of per-hit judgement; runs last, before the tag sequence |
| #980 | Phase 3 | single-site test fix, red on Windows today |
| #985 | Phase 3 | half already fixed on `dev`; the live half is 6 real-state writers in one file |
| #993 | Phase 9 | repo settings, maintainer decision, belongs at the sync it governs |
| #994 | Phase 6 | instrument widening; independent and parallelisable like the rest |

**#996 itself closes when these are fixed** — it is the tracking issue for the
set, and the set is now scheduled rather than unexamined.

> **The process defect #996 names is not fixed by this table.** Chunk 22's bar —
> *every open issue carries a disposition before the tag* — was true when it
> closed and then decayed silently, because nothing re-checks it. All seven above
> were filed **after** that sweep. Re-run the unclaimed-issue query at the start
> of each remaining phase, not once at the end.
